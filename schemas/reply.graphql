# import User from "user.graphql"

scalar DateTime

type Reply {
  id: String!
  sender: User
  type: String
  text: String
  filePath: String
  replies: [Reply]
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
}
