# import Reply from "reply.graphql"

scalar DateTime

type Message {
  id: String!
  channel: Channel
  sender: User
  type: String
  text: String
  filePath: String
  replies: [Reply]
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
}
