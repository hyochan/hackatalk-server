scalar DateTime

type Notification {
  id: ID!
  token: String
  device: String
  os: String
  createdAt: DateTime
  updatedAt: DateTime
}

input NotificationCreateInput {
  token: String!
  device: String
  os: String
}
