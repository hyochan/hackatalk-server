scalar DateTime

enum MemberType {
  OWNER,
  MEMBER
}

type Membership {
  id: ID!
  channel: Channel!
  user: User!
  type: MemberType!
  createdAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
}
